const KEY="viajes_plans_2_data";let db=load(),view="home",activeTripId=null;const $=id=>document.getElementById(id);
function uid(){return Date.now().toString(36)+Math.random().toString(36).slice(2)}function clean(s){return String(s||"").replace(/\r\n/g,"\n").replace(/\r/g,"\n").replace(/\\n/g,"\n")}function esc(s){return clean(s).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}function save(){localStorage.setItem(KEY,JSON.stringify(db))}function load(){try{let s=localStorage.getItem(KEY);if(s)return JSON.parse(s)}catch(e){}return{trips:[],events:[],reservations:[],docs:[]}}function fileToData(f){return new Promise((res,rej)=>{if(!f)return res(null);let r=new FileReader();r.onload=()=>res({name:f.name,type:f.type,size:f.size,data:r.result});r.onerror=rej;r.readAsDataURL(f)})}function dl(name,text){let a=document.createElement("a");a.href=URL.createObjectURL(new Blob([text],{type:"application/json"}));a.download=name;a.click()}function mapLink(t){let u=String(t||"").match(/https?:\/\/\S+/);return u?u[0]:"https://www.google.com/maps/search/?api=1&query="+encodeURIComponent(t||"")}
menuBtn.onclick=()=>{side.classList.add("open");shade.classList.add("show")};closeSide.onclick=shade.onclick=()=>{side.classList.remove("open");shade.classList.remove("show")};closeModal.onclick=()=>modal.classList.remove("show");darkBtn.onclick=()=>document.body.classList.toggle("dark");addTrip.onclick=()=>tripForm();addEvent.onclick=()=>eventForm();addReservation.onclick=()=>reservationForm();addDoc.onclick=()=>docForm();document.querySelectorAll("[data-view]").forEach(b=>b.onclick=()=>{view=b.dataset.view;side.classList.remove("open");shade.classList.remove("show");render()});function showModal(t,h){modalTitle.textContent=t;modalBody.innerHTML=h;modal.classList.add("show")}
function render(){document.querySelectorAll("[data-view]").forEach(b=>b.classList.toggle("active",b.dataset.view===view));screenTitle.textContent={home:"Inicio",trips:"Viajes",events:"Eventos",reservations:"Reservas",docs:"Docs",sync:"Exportar / Importar"}[view];if(view==="home")home();if(view==="trips")trips();if(view==="events")events();if(view==="reservations")reservations();if(view==="docs")docs();if(view==="sync")syncView()}
function home(){app.innerHTML=`<section class=hero><div class=txt><h1>Viajes & Plans 2</h1><p>Viajes, eventos, reservas, documentos y exportación Mac ↔ iPhone</p></div></section><div class=grid><div class=stat><span>${db.trips.length}</span>Viajes</div><div class=stat><span>${db.events.length}</span>Eventos</div><div class=stat><span>${db.reservations.length}</span>Reservas</div><div class=stat><span>${db.docs.length}</span>Docs</div></div><div class=card><h2>Acciones rápidas</h2><button class=primary onclick=tripForm()>+ Viaje</button><button class=primary onclick=eventForm()>+ Evento</button><button class=primary onclick=reservationForm()>+ Reserva</button><button class=primary onclick=docForm()>+ Documento</button><button class=secondary onclick="view='sync';render()">Exportar / importar datos</button></div>`}
function trips(){app.innerHTML=`<div class=card><h2>✈️ Viajes</h2><button class=primary onclick=tripForm()>+ Nuevo viaje</button></div>${db.trips.map(tripCard).join("")||"<div class=empty>Sin viajes.</div>"}`}function tripCard(t){return`<div class=card><h2>${esc(t.name)}</h2><p class=muted>${esc(t.dates)} · ${esc(t.destination)}</p>${t.image?`<img style="max-width:100%;border-radius:18px" src="${t.image.data}">`:""}<p>${esc(t.summary||"")}</p><span class=pill>${(t.notes||[]).length} notas</span><span class=pill>${(t.files||[]).length} archivos</span><div class=actions><button class=secondary onclick="activeTripId='${t.id}';noteForm()">+ Nota</button><button class=secondary onclick="activeTripId='${t.id}';tripFileForm()">+ Archivo</button><button class=secondary onclick="tripForm('${t.id}')">Editar</button><button class=danger onclick="del('trips','${t.id}')">Eliminar</button></div>${(t.notes||[]).map(n=>noteCard(n,t.id)).join("")}${(t.files||[]).map(fileBox).join("")}</div>`}function noteCard(n,tid){return`<div class=noteCard><button class=noteHead onclick="this.parentElement.classList.toggle('open')"><span>📒</span><strong>${esc(n.title)}</strong><span>⌄</span></button><div class=noteBody><div class=noteText>${linkify(esc(n.body))}</div><div class=actions><button class=secondary onclick="noteForm('${tid}','${n.id}')">Editar</button><button class=danger onclick="deleteNote('${tid}','${n.id}')">Eliminar</button></div></div></div>`}
function events(){app.innerHTML=`<div class=card><h2>🎟️ Eventos</h2><button class=primary onclick=eventForm()>+ Nuevo evento</button></div>${db.events.map(e=>`<div class=card><h2>${esc(e.name)}</h2><p class=muted>${esc(e.type)} · ${esc(e.date)} · ${esc(e.place)}</p>${e.link?`<p><a class=mapBtn target=_blank href="${esc(e.link)}">Abrir enlace / entrada</a></p>`:""}${e.file?fileBox(e.file):""}<p>${esc(e.notes||"")}</p><div class=actions><button class=secondary onclick="eventForm('${e.id}')">Editar</button><button class=danger onclick="del('events','${e.id}')">Eliminar</button></div></div>`).join("")||"<div class=empty>Sin eventos.</div>"}`}
function reservations(){app.innerHTML=`<div class=card><h2>📧 Reservas</h2><button class=primary onclick=reservationForm()>+ Nueva reserva</button></div>${db.reservations.map(r=>`<div class=card><h2>${esc(r.name)}</h2><p class=muted>${esc(r.type)} · ${esc(r.date)}</p>${r.locator?`<p><b>Localizador:</b> ${esc(r.locator)}</p>`:""}${r.address?`<p><a class=mapBtn target=_blank href="${mapLink(r.address)}">Abrir Maps</a></p>`:""}${r.file?fileBox(r.file):""}<details><summary><b>Email / texto original</b></summary><div class=noteText>${linkify(esc(r.raw||""))}</div></details><div class=actions><button class=secondary onclick="reservationForm('${r.id}')">Editar</button><button class=danger onclick="del('reservations','${r.id}')">Eliminar</button></div></div>`).join("")||"<div class=empty>Sin reservas.</div>"}`}
function docs(){app.innerHTML=`<div class=card><h2>📂 Documentos</h2><button class=primary onclick=docForm()>+ Añadir documento</button></div>${db.docs.map(d=>`<div class=card><h2>${esc(d.name)}</h2><p class=muted>${esc(d.type)}</p>${d.link?`<p><a class=mapBtn target=_blank href="${esc(d.link)}">Abrir enlace</a></p>`:""}${d.file?fileBox(d.file):""}<p>${esc(d.notes||"")}</p><div class=actions><button class=secondary onclick="docForm('${d.id}')">Editar</button><button class=danger onclick="del('docs','${d.id}')">Eliminar</button></div></div>`).join("")||"<div class=empty>Sin documentos.</div>"}`}
function fileBox(f){if(!f)return"";let img=(f.type||"").startsWith("image/");return`<div class=fileBox><b>📎 ${esc(f.name)}</b><br><a class=mapBtn href="${f.data}" download="${esc(f.name)}" target=_blank>Abrir / descargar</a>${img?`<br><img src="${f.data}">`:""}</div>`}function linkify(txt){return txt.replace(/https?:\/\/[^\s<]+/g,u=>`<a target=_blank href="${u}">${u.includes("maps")||u.includes("google")?"Abrir Maps":"Abrir enlace"}</a>`)}
function tripForm(id=""){let t=id?db.trips.find(x=>x.id===id):{};showModal(id?"Editar viaje":"Nuevo viaje",`<input id=f1 placeholder=Nombre value="${esc(t.name||"")}"><input id=f2 placeholder=Fechas value="${esc(t.dates||"")}"><input id=f3 placeholder=Destino value="${esc(t.destination||"")}"><textarea id=f4 placeholder=Resumen>${esc(t.summary||"")}</textarea><label>Imagen portada</label><input id=f5 type=file accept="image/*"><button class=primary onclick="saveTrip('${id}')">Guardar</button>`)}async function saveTrip(id=""){let t=id?db.trips.find(x=>x.id===id):{id:uid(),notes:[],files:[]};Object.assign(t,{name:f1.value,dates:f2.value,destination:f3.value,summary:f4.value});let f=await fileToData(f5.files[0]);if(f)t.image=f;if(!id)db.trips.push(t);save();modal.classList.remove("show");view="trips";render()}
function noteForm(tid=activeTripId,nid=""){let t=db.trips.find(x=>x.id===tid),n=nid?(t.notes||[]).find(x=>x.id===nid):{};showModal(nid?"Editar nota":"Añadir nota",`<input id=n1 placeholder=Título value="${esc(n.title||"")}"><textarea id=n2 placeholder="Pega nota de Apple Notes">${esc(n.body||"")}</textarea><button class=primary onclick="saveNote('${tid}','${nid}')">Guardar</button>`)}function saveNote(tid,nid=""){let t=db.trips.find(x=>x.id===tid);t.notes=t.notes||[];let n=nid?t.notes.find(x=>x.id===nid):{id:uid()};n.title=n1.value||clean(n2.value).split("\n")[0]||"Nota";n.body=n2.value;if(!nid)t.notes.push(n);save();modal.classList.remove("show");render()}function deleteNote(tid,nid){let t=db.trips.find(x=>x.id===tid);t.notes=t.notes.filter(n=>n.id!==nid);save();render()}function tripFileForm(){showModal("Añadir archivo",`<input id=tf type=file><button class=primary onclick=saveTripFile()>Guardar</button>`)}async function saveTripFile(){let t=db.trips.find(x=>x.id===activeTripId);t.files=t.files||[];let f=await fileToData(tf.files[0]);if(f)t.files.push(f);save();modal.classList.remove("show");render()}
function eventForm(id=""){let e=id?db.events.find(x=>x.id===id):{};showModal(id?"Editar evento":"Nuevo evento",`<input id=e1 placeholder=Nombre value="${esc(e.name||"")}"><select id=e2><option>Concierto</option><option>Teatro</option><option>Deporte</option><option>Entrada</option><option>Otro</option></select><input id=e3 placeholder=Fecha value="${esc(e.date||"")}"><input id=e4 placeholder=Lugar value="${esc(e.place||"")}"><input id=e5 placeholder="URL entrada / enlace" value="${esc(e.link||"")}"><textarea id=e6 placeholder="Notas / email">${esc(e.notes||"")}</textarea><input id=e7 type=file><button class=primary onclick="saveEvent('${id}')">Guardar</button>`);if(e.type)e2.value=e.type}async function saveEvent(id=""){let e=id?db.events.find(x=>x.id===id):{id:uid()};Object.assign(e,{name:e1.value,type:e2.value,date:e3.value,place:e4.value,link:e5.value,notes:e6.value});let f=await fileToData(e7.files[0]);if(f)e.file=f;if(!id)db.events.push(e);save();modal.classList.remove("show");view="events";render()}
function reservationForm(id=""){let r=id?db.reservations.find(x=>x.id===id):{};showModal(id?"Editar reserva":"Nueva reserva",`<input id=r1 placeholder=Nombre value="${esc(r.name||"")}"><select id=r2><option>Hotel</option><option>Restaurante</option><option>Vuelo</option><option>Tren</option><option>Parking</option><option>Entrada</option><option>Otro</option></select><input id=r3 placeholder=Fecha value="${esc(r.date||"")}"><input id=r4 placeholder=Localizador value="${esc(r.locator||"")}"><input id=r5 placeholder="Dirección / Maps" value="${esc(r.address||"")}"><textarea id=r6 placeholder="Pega email de confirmación">${esc(r.raw||"")}</textarea><input id=r7 type=file><button class=primary onclick="saveReservation('${id}')">Guardar</button>`);if(r.type)r2.value=r.type}async function saveReservation(id=""){let r=id?db.reservations.find(x=>x.id===id):{id:uid()};Object.assign(r,{name:r1.value,type:r2.value,date:r3.value,locator:r4.value,address:r5.value,raw:r6.value});let f=await fileToData(r7.files[0]);if(f)r.file=f;if(!id)db.reservations.push(r);save();modal.classList.remove("show");view="reservations";render()}
function docForm(id=""){let d=id?db.docs.find(x=>x.id===id):{};showModal(id?"Editar documento":"Nuevo documento",`<input id=d1 placeholder=Nombre value="${esc(d.name||"")}"><select id=d2><option>PDF reserva</option><option>Entrada</option><option>Captura QR</option><option>Email</option><option>Imagen</option><option>Otro</option></select><input id=d3 placeholder="URL opcional" value="${esc(d.link||"")}"><textarea id=d4 placeholder=Notas>${esc(d.notes||"")}</textarea><input id=d5 type=file><button class=primary onclick="saveDoc('${id}')">Guardar</button>`);if(d.type)d2.value=d.type}async function saveDoc(id=""){let d=id?db.docs.find(x=>x.id===id):{id:uid()};Object.assign(d,{name:d1.value,type:d2.value,link:d3.value,notes:d4.value});let f=await fileToData(d5.files[0]);if(f)d.file=f;if(!id)db.docs.push(d);save();modal.classList.remove("show");view="docs";render()}
function del(c,id){if(confirm("¿Eliminar?")){db[c]=db[c].filter(x=>x.id!==id);save();render()}}function syncView(){app.innerHTML=`<div class=card><h2>🔄 Exportar / Importar</h2><p class=muted>Forma rápida de pasar datos del Mac al iPhone.</p><button class=primary onclick=exportAll()>Exportar todo</button><label>Importar archivo JSON</label><input id=importFile type=file accept="application/json"><button class=primary onclick=importAll()>Importar</button></div>`}function exportAll(){dl("viajes-plans-backup.json",JSON.stringify(db,null,2))}function importAll(){let f=importFile.files[0];if(!f)return alert("Elige el JSON");let r=new FileReader();r.onload=()=>{try{db=JSON.parse(r.result);save();alert("Importado");view="home";render()}catch(e){alert("Archivo no válido")}};r.readAsText(f)}render();